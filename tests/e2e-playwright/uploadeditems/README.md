# Upload Fixtures

Drop test upload assets here. Referenced by file upload specs:

- `sample.jpg` — used by `fileUploadTest.spec.ts` (FU0001/FU0002)

Keep binaries small (<100KB) so repo stays light.
